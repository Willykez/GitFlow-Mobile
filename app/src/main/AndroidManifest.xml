<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed for clone / fetch / push / pull over the network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Repos now live in a PUBLIC folder (/storage/emulated/0/.GitFlow/repos)
        instead of this app's private Android/data folder, so any file
        manager, other app, or PC-over-USB can reach the files. See
        data/PublicStorage.kt for the full reasoning.

        On Android 11+ (API 30+), reading/writing arbitrary public folders
        needs the "All files access" special permission — granted manually
        in system Settings, not a normal runtime popup.
        On Android 10 and below, the classic WRITE_EXTERNAL_STORAGE runtime
        permission is enough.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- For the "new commits available" notification background sync posts.
         Android 13+ (API 33+) treats this as a runtime permission — requested
         from SettingsScreen when background sync is turned on, not at launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets the Actions screen hand a downloaded debug/release APK straight to
         the system installer, instead of just downloading it and stopping there.
         Android still shows its own "allow installs from this app" prompt the
         first time — this permission is what makes that prompt reachable at all. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitFlowMobile"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.GitFlowMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exposes downloaded build artifacts (debug/release APKs from the Actions
             screen) to the system package installer via a content:// URI — Android
             8+ blocks handing a bare file:// path to another app (FileUriExposedException),
             so this is required, not optional, for the "install this APK" flow. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
